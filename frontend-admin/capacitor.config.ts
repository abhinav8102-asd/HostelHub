import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hostelhub.admin',
  appName: 'HostelHubAdmin',
  webDir: 'dist/frontend-admin/browser'
};

export default config;
